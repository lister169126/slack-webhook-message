---
name: Test
run-name: Test something
on:
  push:
  pull_request:

jobs:
  test-runner:
    runs-on: self-hosted
    container:
      image: dockette/vercel:latest
    steps:
      - name: run command
        run: vercel --help
